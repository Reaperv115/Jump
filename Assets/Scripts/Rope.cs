using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Rope : MonoBehaviour
{
    protected float speedX = 0.0f, speedY = 0.0f;
    protected float risingDist = 0.0f, loweringDist = 0.0f;
    protected bool goUp = false, goDown = false;
    protected int numcurrJumps = 0;
    protected Vector2 movement;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
